# Проверка выпуска — 23 сентября 2026

## Локальные регрессии и PostgreSQL

Последние успешные проверки затронутых наборов:

| Набор | Проверки | Среда |
|---|---:|---|
| Сценарии и поиск знаний | 31/31 | Node.js, без API |
| Подтверждённое обращение → оператор | 1/1 | Node.js, без API |
| Адаптеры и аудиособытия | 25/25 | Подставные WebSocket-провайдеры |
| HTTP/WS, cookie, история и жизненный цикл | 12/12 | Локальный HTTP/WS, без API |
| Точные профили моделей | 2/2 | Без API |
| История, включая реальный PostgreSQL | 8/8 | Docker на VPS; PostgreSQL 17 |

Локально тест настоящего PostgreSQL явно пропускается без `HISTORY_TEST_DATABASE_URL`. На VPS он выполнен; дополнительно остановка приложения, перезапуск PostgreSQL и новый запуск приложения сохранили учебную запись, результаты действий и отметку завершения. Другой владелец cookie получил отказ.

## Браузер и реальные провайдеры

- Оба провайдера проверены синтетическим PCM-входом до сценарного ядра и выходного аудио.
- После уточнения схемы SC40 выполнены четыре свежие текстовые Live-сессии. Все озвучки совпали с ответом ядра. Проверка выявила варианты отрицания, которые первоначально давали неправильный факт или `not_found`; исправления покрыты регрессиями на фактически полученных темах.
- Финальная браузерная проверка Gemini: «Объясните, что не покрывает КАСКО» → SC40 → `kb_lookup` → корректные исключения из KB → голосовой ответ и сохранённый транскрипт озвучки.
- Фактически озвученный текст хранится отдельно от проверенного ответа ядра. Это позволяет видеть расхождения и не приписывать модели неозвученный ответ.
- Широкий и узкий интерфейсы проверены; горизонтального переполнения нет. Логотипы загружаются, Noto Sans обслуживается локально. RU/KK glyph coverage проверен во всех трёх начертаниях.
- Физический микрофон участника, акустическое перебивание и медианная задержка по голосовому набору остаются ручными проверками. Их результат не выводится из синтетического smoke-теста.

## Маршрутизация на dev-наборе

Полный результат 103/104 primary и 102/104 full относится к prompt ревизии `44b279c`. После прогона у SC40 явно отмечен обязательный `product_type`; полный показатель новой редакции не измерялся. [Сохранённый отчёт](evaluation-results.json) и [предсказания](evaluation-predictions.json) позволяют пересчитать исходный результат без API. Данные оценки не включаются в runtime prompt.

## Воспроизведение

```sh
npm ci
npm test
python -X utf8 evaluation/evaluate.py docs/evaluation-predictions.json evaluation/dev_utterances.json
```

Настройте `.env` и выполните `docker compose up --build -d` для запуска приложения с PostgreSQL. Реальные Live/API-проверки отдельно описаны в [README](../README.md) и расходуют квоту провайдера. HTTP health check сам по себе не доказывает работу микрофона, голосовой модели или внешнего доступа из любой сети.

{
  "name": "voice-router-doter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Native voice scenario routing for the HackAlem AI Halyk case",
  "engines": {
    "node": ">=22.9.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "test": "node --test",
    "evaluate": "node --env-file-if-exists=.env tools/evaluate.mjs"
  },
  "dependencies": {
    "pg": "^8.23.0",
    "ws": "^8.18.3"
  }
}
